import Box from '@mui/material/Box'
import { styled } from '@mui/material/styles'

export const RouteFallbackRoot = styled(Box)({
  display: 'flex',
  justifyContent: 'center',
  textAlign: 'center',
})
